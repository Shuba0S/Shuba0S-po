{
    "build": {
        "env": {
            "PYTHON_VERSION": "3.12"
        },
        "command": "pip install -r requirements.txt"
    },
    "start": {
        "command": "uvicorn app:app --host 0.0.0.0 --port $PORT"
    }
}
